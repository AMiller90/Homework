#pragma once

#include <iostream>

int main() {


	//Problem 3 
	/*int a, b, c, d, e;



	std::cin >> a;
	std::cin >> b;
	std::cin >> c;
	std::cin >> d;
	std::cin >> e;

	int sum = a + b + c + d + e;


	if (sum > 0) {

		if ((a < b && a < c && a < d && a < e) &&
			(b < c && b < d && b < e && b > a) &&
			(c < d && c < e && c > a && c > b) &&
			(d < e && d > a && d > b && d > c) &&
			(e > a && e > b && e > c && e > d)) {
			std::cout << a << std::endl;
			std::cout << b << std::endl;
			std::cout << c << std::endl;
			std::cout << d << std::endl;
			std::cout << e << std::endl;
		}else 
		if ((b < c && b < d && b < e && b < a) &&
			(c < d && c < e && c < a && c > b) &&
			(d < e && d < a && d > b && d > c) &&
			(e < a && e > b && e > c && e > d) &&
			(a > b && a > c && a > d && a > e)) {
			std::cout << b << std::endl;
			std::cout << c << std::endl;
			std::cout << d << std::endl;
			std::cout << e << std::endl;
			std::cout << a << std::endl;
		}else 
		if ((c < d && c < e && c < a && c < b) &&
			(d < e && d < a && d < b && d > c) &&
			(e < a && e < b && e > c && e > d) &&
			(a < b && a > d && a > e && a > c) &&
			(b > c && b > d && b > e && b > a)) {
			std::cout << c << std::endl;
			std::cout << d << std::endl;
			std::cout << e << std::endl;
			std::cout << a << std::endl;
			std::cout << b << std::endl;
		}else
		if ((d < e && d < a && d < b && d < c) &&
			(e < a && e < b && e < c && e > d) &&
			(a < b && a < c && a > d && a > e) &&
			(b < c && b > e && b > a && b > d) &&
			(c > d && c > a && c > b && c > e)) {
			std::cout << d << std::endl;
			std::cout << e << std::endl;
			std::cout << a << std::endl;
			std::cout << b << std::endl;
			std::cout << c << std::endl;

		}else 
		if ((e < a && e < b && e < c && e < d) &&
			(a < b && a < c && a < d && a > e) &&
			(b < c && b < d && b > a && b > e) &&
			(c < d && c > e && c > a && c > b) &&
			(d > e && d > a && d > b && d > c)) {
			std::cout << e << std::endl;
			std::cout << a << std::endl;
			std::cout << b << std::endl;
			std::cout << c << std::endl;
			std::cout << d << std::endl;

		}
	}
			else if (sum < 0) {


				if ((a < b && a < c && a < d && a < e) &&
					(b < c && b < d && b < e && b > a) &&
					(c < d && c < e && c > a && c > b) &&
					(d < e && d > a && d > b && d > c) &&
					(e > a && e > b && e > c && e > d)) {
					std::cout << a << std::endl;
					std::cout << b << std::endl;
					std::cout << c << std::endl;
					std::cout << d << std::endl;
					std::cout << e << std::endl;
				}
				if ((b < c && b < d && b < e && b < a) &&
					(c < d && c < e && c < a && c > b) &&
					(d < e && d < a && d > b && d > c) &&
					(e < a && e > b && e > c && e > d) &&
					(a > b && a > c && a > d && a > e)) {
					std::cout << b << std::endl;
					std::cout << c << std::endl;
					std::cout << d << std::endl;
					std::cout << e << std::endl;
					std::cout << a << std::endl;
				}
				if ((c < d && c < e && c < a && c < b) &&
					(d < e && d < a && d < b && d > c) &&
					(e < a && e < b && e > c && e > d) &&
					(a < b && a > d && a > e && a > c) &&
					(b > c && b > d && b > e && b > a)) {
					std::cout << c << std::endl;
					std::cout << d << std::endl;
					std::cout << e << std::endl;
					std::cout << a << std::endl;
					std::cout << b << std::endl;
				}
				if ((d < e && d < a && d < b && d < c) &&
					(e < a && e < b && e < c && e > d) &&
					(a < b && a < c && a > d && a > e) &&
					(b < c && b > e && b > a && b > d) &&
					(c > d && c > a && c > b && c > e)) {
					std::cout << d << std::endl;
					std::cout << e << std::endl;
					std::cout << a << std::endl;
					std::cout << b << std::endl;
					std::cout << c << std::endl;
				}

				if ((e < a && e < b && e < c && e < d) &&
					(a < b && a < c && a < d && a > e) &&
					(b < c && b < d && b > a && b > e) &&
					(c < d && c > e && c > a && c > b) &&
					(d > e && d > a && d > b && d > c)) {
					std::cout << e << std::endl;
					std::cout << a << std::endl;
					std::cout << b << std::endl;
					std::cout << c << std::endl;
					std::cout << d << std::endl;

				}
			}
			else if (sum == 0) {

			std::cout << a << b << c << d << e;

		}*/


		system("PAUSE");
		return 0;
	}
